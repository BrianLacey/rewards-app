import { Container, Typography, Grid } from "@mui/material";

const Result = () => {
  return (
    <Container className="text-white">
      <Grid container className="pt-20 justify-center">
        <Grid>
          <Typography variant="h2">I am the Result page!</Typography>
        </Grid>
      </Grid>
    </Container>
  );
};

export default Result;
